# Портфолио Троянова — передача контекста

Обновлено: 2026-09-09. Этот файл заменяет `PORTFOLIO_HANDOFF.md` (там устаревший акцентный цвет).

## Стек и структура

Не «просто HTML» — проект переехал на React + Vite, но кейсы остались статикой.

- **Главная** — React: `src/App.jsx`, `src/home.css`, `src/home-assets.js`. Точка входа `index.html`.
- **Кейсы** — статические страницы: `artifact.html` + `artifact.css`, `ai-producer.html` + `ai-producer.css`. Общие стили в `styles.css`.
- **Общее для всех** — `bottom-nav.css` (нижняя навигация), `assets/reveal.css` + `assets/reveal.js` (появление блоков), `smooth-scroll.js` (Lenis), `media-playback.js`.
- Мультистраничная сборка настроена в `vite.config.js` — все три страницы в `rollupOptions.input`.
- Зависимости: `react`, `react-dom`, `three`, `@react-three/fiber`, `lenis`.

## Токены (актуальные)

- Фон `#FFFFFF`, основной текст `#111111`, вторичный `#6D6D68`, приглушённый `#888888`
- **Акцент `#C3009E`** — магента. В старом файле передачи указан `#D8FF9D`, это неверно.
- Обводка контролов `rgba(17,17,17,0.62)` — токен `--nav-border` / `--home-control-border`
- Шрифт `Bounded`. Контент 1200px в кадре 1440, поля по 120.

## Источник правды в Figma

Файл `urnwqlmbMmlXtvo36amBhe`.

- Главная: `11:2` (Desktop - 1)
- ARTIFACT: десктоп `2048:560` (Desktop - 9), мобильный `iPhone 13 & 14 - 2`
- AI PRODUCER: `1029:2190`
- О себе: `1926:353` (Desktop - 7) — **в код не перенесена**
- Компонент Button: `1700:321` (4 варианта, состояния)
- Попап контактов: `1639:294`

## Что уже сделано и трогать осторожно

- **Нижняя навигация** сведена в один `bottom-nav.css` с контрактом переменных `--nav-*`. Каждая сторона маппит на них свои токены. Правка панели теперь в одном месте.
- **Прогрессивное размытие** нижней кромки: 4 слоя с геометрическим ростом радиуса и полосовыми масками. Не «один слой с маской» — иначе видны ступени.
- **ARTIFACT переведён с координат на поток.** Раньше секции имели прибитые высоты и абсолютное позиционирование, правка текста ломала раскладку. Осталось 35 абсолютных элементов — это композиция (слои мокапов, наложенные стрелки), их трогать не надо.
- **Стрелки** сведены к 2px. Важно: толщина в SVG запечена в контур, и у файлов стоит `preserveAspectRatio="none"` — растяжение по ширине меняет толщину. Размеры в CSS должны совпадать с пропорциями файла.
- **Появление блоков при прокрутке** — `assets/reveal.js`: класс `.rv` навешивается по списку селекторов, `.in` добавляется через IntersectionObserver. Механика скопирована с lazimjarif.com.
- **Изображения**: шоты пережаты с 13.9 MB до 0.24 MB (WebP под ретину). Такой же перебор возможен в мокапах кейсов — не проверялось.

## Открытые задачи

1. Страница «О себе» — есть в Figma (`1926:353`, бенто из видео без текста), в коде нет. На главной пока блок с текстом про насмотренность.
2. TAYA — помечена «Soon», кейс не начат.
3. В `ai-producer.html` две развилки, где на десктопе и мобилке разный текст: рубрика «ГЛУБИННЫЕ ИНТЕРВЬЮ» / «ПРОВЕРКА» и абзац в тестировании.
4. Индикатор баланса в ARTIFACT: в исходном скриншоте `split-after.png` зелёное кольцо упирается в правый край. Нужна пересъёмка кадра с отступом — из имеющегося материала не поправить.
5. Мобильная версия «Тестирования» переведена в поток, но композиция внутри всё ещё на координатах.

## Правила совместной работы

- **Версии в ссылках на CSS обязательны.** Браузер держит файлы в кеше, и правка «не приезжает». Меняешь css — меняй `?v=` в подключении. Это уже несколько раз съедало время.
- SVG и картинки тоже кешируются: при замене файла добавляй версию в `src`.
- Перед правкой файла проверь, не изменил ли его второй агент — работаем параллельно и уже дважды затирали друг друга.
